language: r
sudo: false
cache: packages
dist: trusty

r-github-packages:
  - jimhester/covr


after_success:
    - travis_wait 20 tar -C .. -xf $PKG_TARBALL
    - travis_wait 40 Rscript -e 'covr::coveralls(line_exclusions= c("/src/include/models.hpp", "src/*.cpp", "R/zzz.R", "R/runshiny.R"))'
